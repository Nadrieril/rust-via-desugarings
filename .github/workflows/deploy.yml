name: Deploy mdBook

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: '0.4.52'

      - name: Install mdbook-backlinks
        uses: baptiste0928/cargo-install@v3
        with:
          crate: mdbook-backlinks
          git: https://github.com/Nadrieril/mdbook-backlinks
          rev: 2fb722cd5df25a0d7d5d250910dd0bbc428e61e5

      - name: Build book
        run: cd src && mdbook build

      - name: Upload built book
        uses: actions/upload-pages-artifact@v3
        with:
          path: src/.book-build/html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
