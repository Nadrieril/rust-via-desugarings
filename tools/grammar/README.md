# Custom grammar language

This is a library that parses a custom grammar language and:
- A markdown pretty-printer for it;
- Generates railroad diagrams;
- Converts it to rustylr to make it into a real parser.

Adapted from the tooling for the [Rust
Reference](https://github.com/rust-lang/reference/blob/master/tools)
using an LLM with little supervision.
